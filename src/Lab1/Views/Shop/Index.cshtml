@model IEnumerable<Shop>
@{
    ViewBag.Title = "Shops";
}
<div><h1>@ViewBag.Title</h1></div>
@foreach (Shop shop in @Model)
{
    <div><b>@shop.Name</b></div>
    <div>@shop.Address</div>
    <div>Contracts: @shop.Contracts.Count
    </div>
}